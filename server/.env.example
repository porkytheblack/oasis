# Server Configuration
PORT=3000
NODE_ENV=development

# Database (PostgreSQL)
# Format: postgresql://[user]:[password]@[host]:[port]/[database]
# For local development with docker-compose, use:
DATABASE_URL=postgresql://oasis_user:oasis_password@localhost:5432/oasis

# CORS (comma-separated origins, or * for all)
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# Base URL for download URLs (used when generating artifact URLs)
BASE_URL=http://localhost:3000

# Cloudflare R2 Storage Configuration
# R2 endpoint URL (format: https://<account-id>.r2.cloudflarestorage.com)
R2_ENDPOINT=https://your-account-id.r2.cloudflarestorage.com
# R2 access key ID from Cloudflare API tokens
R2_ACCESS_KEY_ID=your-r2-access-key-id
# R2 secret access key from Cloudflare API tokens
R2_SECRET_ACCESS_KEY=your-r2-secret-access-key
# R2 bucket name
R2_BUCKET=oasis-releases
# Optional: Public URL for R2 bucket (if configured with public access or custom domain)
# If set, public URLs will be returned instead of presigned URLs for downloads
R2_PUBLIC_URL=
